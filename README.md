# Yoko — Works

GitHub Pages向けの静的ポートフォリオです。ビルド作業は不要です。

## 公開方法

1. このフォルダの中身をGitHubリポジトリへ置く
2. GitHubの `Settings → Pages` を開く
3. `Deploy from a branch` を選び、対象ブランチの `/ (root)` を指定する

作品の追加は `index.html` 内の `.project` ブロックを複製して、URL・タイトル・説明を書き換えます。
